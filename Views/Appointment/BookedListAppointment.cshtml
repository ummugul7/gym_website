@model IEnumerable<proje.Models.Appointment>

@{
    ViewData["Title"] = "ListAppointment";
}

<h3>ListAppointment</h3>
@if (TempData["Msj"] != null)
{
    <p>@TempData["Msj"]</p>
}

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Time)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Member.UserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Coach.member.UserName)
            </th>
           
            <th></th>

            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Time)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Member.UserName)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.Coach.member.UserName)
            </td>
            
            <td >

   

    <a asp-action="Details"  asp-route-id="@item.Id"  class="btn btn-sm btn-success">
        Confirm
    </a>

    <a asp-action="Delete"  asp-route-id="@item.Id"  class="btn btn-sm btn-danger"
       onclick="return confirm('Are you sure you want to delete this item?');">
        Delete
    </a>
</td>
                <td>@if (item.IsConfirmed == false)
                    {
                    }
                    else
                    {
                        <span class="text-success">Confirmed</span>
                    }
                </td> 
            </tr>
}
    </tbody>
</table>
